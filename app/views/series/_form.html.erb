<%= form_with(model: series, local: true) do |form| %>
  <% if series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
      <% series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :series_name %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year, id: :series_year %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :series_description %>
  </div>

  <div class="field">
    <%= form.label :total_qty %>
    <%= form.text_field :total_qty, id: :series_total_qty %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
